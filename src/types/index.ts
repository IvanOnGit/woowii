export interface DataPoint {
  subject: string
  value: number
}

export interface TooltipProps {
  active?: boolean;
  payload?: { name: string; value: number }[];
}
export interface DataPoint {
  subject: string
  value: number
}